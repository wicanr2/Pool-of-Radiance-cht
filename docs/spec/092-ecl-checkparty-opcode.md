# Spec 092：`1Eh CHECKPARTY`

狀態：READY（三種模式都讀完並實作）。日期：2026-09-03。

## 它寫四個值（overlay-03 `1512h`，6 個運算元）

收尾的 `14B2h` 把四個結果寫進運算元 3..6：

| 運算元 | 值 |
|---|---|
| 3 | 最小值（初始 FFh）|
| 4 | 最大值（初始 0）|
| 5 | 平均（總和整除人數）|
| 6 | 旗標（只有效果模式會設）|

初始值要照抄：空隊伍寫回去的是 FFh／0／0／0，那是原版的初始值而不是
「沒算」。

## 三種模式，由運算元 1 決定

`1530h` 先看 `[6DCCh]`：等於 1 就取運算元 1 的**位址**，否則取它的**值**。
接著比對：

| 運算元 1 | 模式 |
|---|---|
| 字面值 0 | 問隊上有沒有人掛著運算元 2 那個效果碼 |
| 位址 `6BA7h` | 統計記錄 `+79h`，也就是賊技能的「找／解陷阱」（spec 095）|
| 位址 `6C1Bh` | 統計記錄 `+11Ch`（移動力，spec 079）|

效果模式走 overlay-25 entry 27（`21DCh`）：沿角色的效果串列
（spec 069，`+7Fh` 是頭、節點 `+0` 是效果碼、`+5` 是下一個）找那個碼，
找到第一個就停。

## 四個呼叫點

| 位置 | 模式 | 運算元 2 |
|---|---|---|
| `ECL2/b20 @1B88h` | `6C1Bh` 移動力 | 0 |
| `ECL3/b14 @1AA2h` | `6BA7h` 找／解陷阱 | 0 |
| `ECL3/b14 @1AB4h` | 效果 | 19 |
| `ECL8/b13 @14B4h` | 效果 | 19 |

## remake 現況

效果模式與移動力模式已接。效果串列在存檔裡是 `Character.Effects`，
法術還沒接上來所以目前一律是空的——**空的是正確答案，不是佔位**。

`6BA7h` 那一支也接了：`+79h` 是賊技能的「找／解陷阱」（spec 095）。
remake 還沒有賊技能的產生端，非賊本來就是 0，所以目前一律得到 0／0／0——
那是正確答案，不是佔位。
