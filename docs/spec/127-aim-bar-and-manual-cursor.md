# Spec 127：瞄準列與 Manual 格子游標（overlay-13 `352Ch`）

狀態：READY（選項列的字串與五個字母各自跳去哪、Manual 的入口與游標的
移動方式）；DRAFT（游標迴圈的離開條件、`013D:0061h` 畫的是什麼、
`T` 那一支）。
日期：2026-09-06。

## 選項列

字串在 overlay-13，四段接起來：

| 位址 | 長度 | 內容 |
|---|---:|---|
| `2A44h` | 8 | `Range = ` |
| `2A50h` | 7 | `Target ` |
| `2A58h` | 17 | `Next Prev Manual ` |
| `2A6Ah` | 11 | `Center Exit` |
| `2A76h` | 4 | `Aim:` |

`2B96h` 把 `2A58h` 與 `2A6Ah` 接成一條，提示語用 `2A76h`，交給
`011D:002Fh`——**和鎖住的門那個選單是同一支選單常式**（spec 122）。
所以瞄準時底下那一列是

```
Aim: Next Prev Manual Center Exit
```

## 五個字母跳去哪

`352Ch` 的主迴圈拿回字母之後逐個比：

| 位址 | 字母 | 去哪 |
|---|---|---|
| `365Bh` | `N` | 下一個目標（就地改 `[bp-0DCh]` 那一組游標欄位）|
| `366Ch` | `P` | 上一個目標 |
| `367Dh` | `M` | **Manual**：`call 2DD3h`，回來之後 `010A:0093h` 重畫、`call 32C3h` 更新，並把 `[bp-0DDh]` 清成 0 |
| `36CDh` | `T` | `call 2C17h`——把目前這一格收成目標（`2C17h` 先看 `[bp+0Eh] == 1` 時跑 `2977h` 的可打判定，不成立就把 `[bp+0Ah]` 指到的旗標清成 0）|
| `3714h` | `C` | Center |

`E`（Exit）不在比較裡，是「都不match」的落點。

## Manual 的游標（`2DD3h`）

```
2DDA  05BB:16B5(記錄, 7, 0)              ; 字串／畫面前置
2DEB  X = 013D:006Bh(記錄)               ; 目前目標那一格
2DF9  Y = 013D:0070h(記錄)
2E07  [bp-2Ah] = 20h                     ; 目前按鍵，初值是空白
2E0B  [bp-2Fh] = 8                       ; 目前方向，8 ＝ 原地（九方向表）
2E16  DS:6674h 指到的地圖 +4／+5 都設成 1
2E28  迴圈：Pos([bp-2Ah], cs:2D80h)      ; cs:2D80h 是長度 1 的字串 " "
2E43  013D:0061h(方向, 3, Y, X)          ; overlay-32 entry 13，畫游標
2E4F  X += DS:274Ah[方向]                ; 九方向位移表，與雲、開門共用
      Y += DS:2753h[方向]
```

**游標就是靠那張九方向位移表走的**——與臭雲的四格（spec 121）、
開門時算對面那一格（spec 122）是同一張表。方向的初值 8 是「原地」，
所以進來的時候游標停在目前的目標上。

## 還沒讀

- **迴圈的離開條件。** `2E28h` 用 `05BB:08D4h`（Turbo Pascal 的 `Pos`）
  拿目前按鍵去比 `cs:2D80h`，而那是一個只有一個空白的字串。
  照字面是「按空白才怎樣」，但旗標的方向沒有逐條驗過，
  **不要照這一段直接接**。
- `013D:0061h`（overlay-32 entry 13）畫的到底是什麼。火球術的
  overlay-22 `26EEh` 也呼叫它（`013D:0061h(6CADh, 6CAEh, 0, 8)`），
  所以它多半是「在某一格畫一個記號」，但沒讀。
- `T` 那一支為什麼在選項列上看不到——`Next Prev Manual Center Exit` 沒有 `T`。
  `2DA1h`／`2DB9h` 還有第二份 `Range = `／`Target ` 字串，可能是另一個變體。

## remake 的現況

`cmd/pool-game` 的挑目標只有 `N`／`P`／Enter（spec 074 的模式 4／6／7／0Fh），
**沒有 Manual 也沒有 Center**。接 Manual 要先把上面那三項讀完，
不然游標會走得像但停不下來。
