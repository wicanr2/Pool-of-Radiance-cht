# Spec 127：瞄準列與 Manual 格子游標（overlay-13 `352Ch`）

狀態：READY（選項列的字串、五個字母各自跳去哪、Manual 的入口、游標的八個
方向鍵、確定與取消各自做什麼）；DRAFT（迴圈頭那一個旗標的極性、
`013D:0061h` 畫的是什麼）。
日期：2026-09-06。

## 選項列

字串在 overlay-13，四段接起來：

| 位址 | 長度 | 內容 |
|---|---:|---|
| `2A44h` | 8 | `Range = ` |
| `2A50h` | 7 | `Target ` |
| `2A58h` | 17 | `Next Prev Manual ` |
| `2A6Ah` | 11 | `Center Exit` |
| `2A76h` | 4 | `Aim:` |

`2B96h` 把 `2A58h` 與 `2A6Ah` 接成一條，提示語用 `2A76h`，交給
`011D:002Fh`——**和鎖住的門那個選單是同一支選單常式**（spec 122）。
所以瞄準時底下那一列是

```
Aim: Next Prev Manual Center Exit
```

## 五個字母跳去哪

`352Ch` 的主迴圈拿回字母之後逐個比：

| 位址 | 字母 | 去哪 |
|---|---|---|
| `365Bh` | `N` | 下一個目標（就地改 `[bp-0DCh]` 那一組游標欄位）|
| `366Ch` | `P` | 上一個目標 |
| `367Dh` | `M` | **Manual**：`call 2DD3h`，回來之後 `010A:0093h` 重畫、`call 32C3h` 更新，並把 `[bp-0DDh]` 清成 0 |
| `36CDh` | `T` | `call 2C17h`——把目前這一格收成目標（`2C17h` 先看 `[bp+0Eh] == 1` 時跑 `2977h` 的可打判定，不成立就把 `[bp+0Ah]` 指到的旗標清成 0）|
| `3714h` | `C` | Center |

`E`（Exit）不在比較裡，是「都不match」的落點。

## Manual 的游標（`2DD3h`）

```
2DDA  05BB:16B5(記錄, 7, 0)              ; 字串／畫面前置
2DEB  X = 013D:006Bh(記錄)               ; 目前目標那一格
2DF9  Y = 013D:0070h(記錄)
2E07  [bp-2Ah] = 20h                     ; 目前按鍵，初值是空白
2E0B  [bp-2Fh] = 8                       ; 目前方向，8 ＝ 原地（九方向表）
2E16  DS:6674h 指到的地圖 +4／+5 都設成 1
2E28  迴圈：Pos([bp-2Ah], cs:2D80h)      ; cs:2D80h 是長度 1 的字串 " "
2E43  013D:0061h(方向, 3, Y, X)          ; overlay-32 entry 13，畫游標
2E4F  X += DS:274Ah[方向]                ; 九方向位移表，與雲、開門共用
      Y += DS:2753h[方向]
```

**游標就是靠那張九方向位移表走的**——與臭雲的四格（spec 121）、
開門時算對面那一格（spec 122）是同一張表。方向的初值 8 是「原地」，
所以進來的時候游標停在目前的目標上。

## 游標迴圈的全貌

```
2E28  迴圈頭：Pos([bp-2Ah], cs:2D80h)   ; cs:2D80h 是長度 1 的字串 " "
2E38    → 離開（32BDh，retf 16h）
2E43  013D:0061h(方向, 3, Y, X)          ; 在那一格畫游標
2E4F  X += DS:274Ah[方向]；Y += DS:2753h[方向]
…
3151  011D:002Fh(DS:6D24h 的選單, …)     ; 讀一個鍵，存進 [bp-2Ah]
315C  Return（0Dh）或 T（54h）→ 3167h    ; **確定這一格**：地圖 +4 清 0
3217  以下逐個比方向鍵，設 [bp-2Fh]
326A  0 或 E（45h）→ 3272h               ; **取消**：013D:0043h 還原那一格、
                                          [bp+0Ah] 指到的旗標清 0
32B0／32B6  方向 = 8（原地）
32BA  jmp 2E28                           ; 回迴圈頭
```

### 方向鍵就是戰術移動那八個

| 鍵 | 碼 | 方向 |
|---|---:|---:|
| `H` | 48h | 0 |
| `I` | 49h | 1 |
| `M` | 4Dh | 2 |
| `Q` | 51h | 3 |
| `P` | 50h | 4 |
| `O` | 4Fh | 5 |
| `K` | 4Bh | 6 |
| `G` | 47h | 7 |

**與 spec 053 的 Move 命令逐鍵相同**（remake 的 `tacticalStepKeys` 就是這一組），
所以瞄準的游標與走路用同一套手感，不是另一組鍵。

`Return` 與 `T` 都是「就選這一格」，`E` 與掃描碼 0 都是取消——
取消時先 `013D:0043h` 把那一格還原，再把回傳旗標清成 0。
這也解釋了外層 `36CDh` 那個看起來多出來的 `T`：**它與 Return 是同一件事**。

## 還沒讀

- **迴圈頭那一個旗標的極性。** `2E31h` 用 `05BB:08D4h`（Turbo Pascal 的 `Pos`）
  拿 `[bp-2Ah]` 去比一個只有空白的字串，`2E36h` 的 `je` 決定要不要繼續。
  靜態讀不出來：`Pos` 之後沒有 `or al,al` 這類設旗標的指令，`je` 吃的是
  RTL 最後一道運算留下的旗標。**而且兩種讀法都會矛盾**——`[bp-2Ah]` 的初值
  就是空白（`2E07h`），「空白就離開」的話 Manual 一次都跑不起來；
  「空白才繼續」的話按方向鍵就會離開。
  要嘛 `05BB:08D4h` 不是 `Pos`，要嘛還有一條沒讀到的路徑會把 `[bp-2Ah]`
  重設回空白。**這一格要用實跑或讀 RTL 才收得掉，不要猜著接。**
- `013D:0061h`（overlay-32 entry 13）畫的到底是什麼。火球術的 overlay-22
  `26EEh` 也呼叫它（`013D:0061h(6CADh, 6CAEh, 0, 8)`），所以它多半是
  「在某一格畫一個記號」，但沒讀。

## remake 的現況

`cmd/pool-game` 的挑目標只有 `N`／`P`／Enter（spec 074 的模式 4／6／7／0Fh），
**沒有 Manual 也沒有 Center**。八個方向鍵、Return／`T` 確定、`E` 取消
這三件事已經可以照接；**卡住的是迴圈的離開條件**——接錯的症狀是游標
走得像但停不下來，而那在測試裡看起來像逾時，不像接錯。
