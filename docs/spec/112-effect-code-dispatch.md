# Spec 112：效果代碼的分派（overlay-24 entry 3／entry 1 與 `014Dh`）

狀態：READY（三支的簽章、二十組的代碼表、有作用範圍的那四個代碼、
`DS:6786h` 那張表怎麼填、否決 `DS:677Ch` 的四個代碼，以及群組編號的意思
——效果系統就是原版套用命中與豁免修正的機制——都讀出來了；
115 支處理常式已有機器盤點；怪物命中後的群組 2／3、`55h`／`56h`
能量吸取，`0Ch`／`26h` 力量效果的重疊與到期收尾，以及祝福／詛咒的群組 10 與
急速／緩速的群組 18 已接入 remake；群組 10／16 除 `2Fh` 外的十四個碼、群組 9 的十個碼
與群組 17 的讀取端也逐條讀過並接上；群組 11／12／6 裡屬於只掛效果那一批法術的碼接上；
群組 12 除 `3Dh 6Fh 7Dh` 外全部、群組 6 的 `0Ah 14h 1Ch`、群組 4 的 `1Dh`、群組 5 的 `1Ch`
與 entry 7 的 byte 無號比較也接上；其餘碼列在 OPEN）。
日期：2026-09-13；2026-09-26 群組 10 的 `01h`／`02h`、群組 18 三個碼、entry 15／20（#81）；
同日群組 9／10／16／17（#86）；同日群組 11／12／6 的法術碼（#89）；同日群組 12 其餘、
群組 6／4／5（#96／#99）。

## 為什麼要讀這一支

spec 096 把 overlay-24 entry 3（`02E2h`，2051 bytes）列成「還沒讀」，而怪物
AI 的「這個候選能不能打」（overlay-13 `1087h`）整個靠它：`1087h` 只是把
`DS:677Ch` 清成 0、問兩次、再看那個 byte 有沒有被立起來。

```
1087  f(候選: far; 自己: far): boolean      ; retf 8
1091  候選為 NULL → 回 false
109c  候選就是自己 → 回 true
10b1  DS:677Ch = 0
10b6  0100h:002Fh(1, 候選)                  ; overlay-24 entry 3，群組 1
10c4  DS:677Ch != 0 → 1117h
10cb  存下自己 runtime +0Ah／+0Ch（目前的目標）
10df  把目標暫時換成這個候選
10f4  0100h:002Fh(0, 自己)                  ; 群組 0
1102  還原 +0Ah／+0Ch
1117  DS:677Ch 仍為 0 → 回 true，否則 false
```

群組 0 只有一個代碼（`7Eh`），而且是**對自己**問的——先把自己的目標指向候選
才問，所以 `7Eh` 的處理常式讀的是行動者當下的目標。

## 三支的分工

| 位置 | 簽章 | 做什麼 |
|---|---|---|
| overlay-24 entry 3（`02E2h`）| `f(群組: byte; 記錄: far)`，`retf 6` | 純表：對群組裡的每個代碼各呼叫一次 `014Dh` |
| overlay-24 `014Dh`（近呼叫）| `f(代碼: byte; 記錄: far)`，`retf 6` | 判斷這個代碼代表的效果會不會影響到這筆記錄 |
| overlay-24 entry 1（`0000h`）| `f(代碼: byte; 記錄: far; 節點: far; 模式: byte)`，`retf 0Ch` | 用代碼查 `DS:6786h` 的遠指標表，把 (記錄, 節點, 模式) 交給那個代碼自己的處理常式 |

`02E2h` 是一個 20 路的 `case`，除了選群組之外沒有任何判斷——2051 bytes 幾乎
全是 140 次相同形狀的呼叫。

## `014Dh`：這個效果影響得到誰

```
014d  f(代碼: byte; 記錄: far)
0154  命中 = 0
0159  走訪指標 = DS:5CF4h                    ; 戰鬥者串列頭
0167  overlay-25 entry 27(記錄, 代碼, &節點)  ; ＝ 21DCh，spec 059 的效果串列走訪
017e    有 → 命中 = 1，跳到收尾
0188  代碼不在 CS:012Dh 那個集合裡 → 跳到收尾（命中仍為 0）
019b  把 DS:6679h 起的 0D8h bytes 存進區域緩衝
01af  對串列上的每一個戰鬥者：
01bc    命中已經是 1 → 停
01c6    overlay-25 entry 27(這一個, 代碼, &節點) → 沒有就換下一個
01e4    DS:4954h != 5 → 命中 = 1              ; 不在戰術地圖上就不算距離
01ee    半徑 = (代碼 == 31h) ? 6 : 1
0200    x／y／體型 = overlay-32 entry 15／16／17(這一個)
0234    近鄰查詢(DS:6674h 的戰術地圖, x, y, 半徑, 0FFh, 體型)
0241    n = DS:6678h；逐筆比 DS:[6676h + i × 3] 是不是「我」的索引
027c      是 → 命中 = 1
0292    走訪指標 = 這一個的 +104h            ; 串列的下一個
02a8  還原 DS:6679h 的 0D8h bytes
02bc  命中 → overlay-24 entry 1(代碼, 記錄, 節點, 0)
02df  retf 6
```

兩條路：**自己身上帶著**這個效果，或者**站在別人的作用範圍裡**。後者只對
`CS:012Dh` 這個集合裡的四個代碼成立——`15h`、`2Dh`、`2Eh`、`31h`，其中 `31h`
的半徑是 6，其餘是 1。集合常數是 Turbo Pascal 的 32-byte 位元集，逐位元展開
就是這四個值。

`019Bh` 與 `02A8h` 把 `DS:6679h` 起的 216 bytes 存下來又還原，是因為中間的
近鄰查詢會覆寫那張表（spec 056 的鄰近查詢結果表就在 `DS:6676h`）——呼叫端
可能正拿著那張表在用。

## `DS:6786h` 那張表

`call dword ptr [di+6786h]` 裡的 `6786h` 是 **Turbo Pascal 的偏移基底**，不是
表的第一格。表宣告成 `array[1..N] of far pointer`，實際落在 `678Ah`，編譯器把
基底寫成 `678Ah − 4 × 1 = 6786h`（`combat.DisablingEffectCodes` 的
`DS:2880h` 折疊基底 `287Fh`、索引 1..4 是同一個手法）。所以 `6786h..6789h`
那四個 byte 是別的變數——overlay-12 `0152h` 就把 `DS:6784h`／`6786h` 當成
「目前目標」的遠指標在用。**看到 `[di + 常數]` 的表不要假設常數就是第一格。**

填表的是 **overlay-12 entry 1（`31B2h`）**，一長串直線寫入：

```
31b5  ax = 0039h；dx = 006Ch
31bb  DS:678Ah = ax；DS:678Ch = dx      ; 代碼 1 → 006Ch:0039h
31c2  ax = 003Eh；…                     ; 代碼 2 → 006Ch:003Eh
…
```

`006Ch` 是 overlay-12 自己的 stub segment（spec 109），偏移換算回進入點是
`(offset − 20h) ÷ 5`。整支填了 **138 格，代碼 1 到 139**；漏掉的 `58h` 由
**overlay-22 `3760h`** 另外填。處理常式的落點：136 個在 overlay-12，
2 個在 overlay-13（代碼 `49h`、`4Ah`，`0096h` 段）。

處理常式的簽章是 `f(記錄: far; 節點: far; 模式: byte)`，`retf 0Ah`。
**模式有兩個值**：`014Dh` 命中時傳 0（套用），overlay-24 entry 2（`0028h`）
把節點摘掉之前傳 1（收尾）。entry 2 順帶把效果串列的版面補齊：節點 9 bytes，
`+0` 代碼、`+4`「有收尾常式」旗標、`+5`／`+7` 下一個的遠指標，串列頭在記錄
`+7Fh`／`+81h`，釋放走 RTL 的 FreeMem。

## `DS:677Ch` 是誰立起來的

不是那三支分派。全 36 顆 overlay 與 `START.EXE` 掃過 `A2 7C 67`／
`C6 06 7C 67`／`A0 7C 67`／`80 3E 7C 67` 四種存取形狀：

| 檔案 | 寫 | 讀 |
|---|---|---|
| overlay-12 | `0946h`（entry 25）、`0C52h`（entry 35）、`173Ah`（entry 66）、`2ECAh`（entry 120）| `2E84h`（entry 120）|
| overlay-13 | `10B1h`（`1087h` 清成 0）| `10C4h`、`1117h` |

拿表一查，那四個進入點就是四個效果代碼：

| 進入點 | 代碼 | 做什麼 |
|---|---|---|
| entry 25（`0927h`）| `19h` | 問 `DS:5CF0h` 指到的那一位有沒有效果 `18h`，**沒有**就 `677Ch = 1`；接著 `DS:6780h −= 4`（無條件）|
| entry 35（`0C40h`）| `25h` | 記錄的 runtime `+3`（先攻分數）大於 0 → `677Ch = 1`、`DS:6780h = FFh` |
| entry 66（`1737h`）| `47h` | 直接 `677Ch = 1`、`DS:6780h −= 4` |
| entry 120（`2E50h`）| `7Eh` | 走**目標**（`+108h` 的 `+0Ah`）的物品串列（`+0C8h` 起、`+2Ah` 接下一個），某一格的 `+2Eh + i` 是 `98h` 而 `+31h` 是 `FCh`，或 `+2Eh + i` 是 `76h` → `677Ch = 1` |

於是 `1087h` 的「能不能打」完全展開了：

- 群組 1 問候選：`19h`、`25h`、`47h` 三個會否決，**`3Fh` 不會**——它指到
  overlay-12 entry 126（`31A9h`），那是一支 `push bp; mov bp,sp; mov sp,bp;
  pop bp; retf 0Ah` 的**空常式**（代碼 `05h` 也指到它）。
- 群組 0 問自己：`7Eh` 讀的是**自己當下的目標**，所以 `1087h` 才要先把
  `+0Ah`／`+0Ch` 暫時換成候選再問（`10CBh`..`1102h`）。

`DS:6780h` 是這些常式共用的另一個累加格（entry 66 減 4、entry 35 寫 `FFh`、
overlay-12 `0165h` 加 2），語意還沒對。

## 二十個群組

`[bp+0Ah]` 的值就是群組編號。代碼依原版的呼叫順序列出（順序有意義：`014Dh`
一命中就會叫處理常式，所以先問到的先生效）。

| 群組 | 代碼 |
|---:|---|
| 0 | `7Eh` |
| 1 | `3Fh 25h 19h 47h` |
| 2 | `55h 56h 57h 44h 4Fh 50h 4Ch 49h 4Dh` |
| 3 | `40h 41h 42h 43h 44h 45h 46h 4Fh 55h 56h 57h` |
| 4 | `1Dh 03h 06h` |
| 5 | `1Ch 29h 68h 78h 65h 73h 74h 77h 7Bh 60h 5Eh 3Ch 7Ah 75h` |
| 6 | `71h 3Dh 7Ah 3Ch 5Bh 0Ah 14h 69h 6Ah 70h 72h 76h 11h 5Dh 65h 1Ch` |
| 7 | `33h 34h 35h 1Fh` |
| 8 | `63h 51h 52h 59h 48h 38h` |
| 9 | `69h 6Ah 6Bh 6Ch 6Dh 6Eh 6Fh 70h 7Ch 7Dh` |
| 10 | `01h 02h 21h 24h 31h 03h 06h 12h 1Ah` |
| 11 | `21h 11h 08h 09h 2Dh 2Eh 1Eh` |
| 12 | `08h 09h 0Ah 11h 14h 21h 24h 2Dh 2Eh 31h 3Dh 6Fh 7Dh 5Ah 61h` |
| 13 | `63h 64h 67h 4Bh 4Ah` |
| 14 | `53h 54h 58h 79h` |
| 15 | `15h 1Eh 4Ah 4Bh 0Bh` |
| 16 | `19h 47h 25h 2Fh 30h 59h` |
| 17 | `01h 02h 0Bh` |
| 18 | `27h 2Ah 3Ah` |
| 19 | `62h 17h 48h 38h 0Bh` |

**正對照**：群組 7 是 `33h 34h 35h 1Fh`，與 `DS:2880h..2883h` 那四個
「反應攻擊否決」代碼逐位元組相同、順序也一樣（spec 059、
`combat.DisablingEffectCodes`），而呼叫群組 7 的正是 overlay-08 `021Ah`——
兩份各自解出來的東西對上，表示這張表抽對了。

## 群組在做什麼

把呼叫端逐個讀出來之後，群組編號就有了名字——**效果系統就是原版套用戰鬥
修正的機制**，`0100h:002Fh` 是那個掛勾點：

| 群組 | 呼叫端 | 意思 |
|---:|---|---|
| 0 | overlay-13 `10FDh` | AI 判「我能不能打這個候選」時問**自己**（`7Eh` 讀的是自己當下的目標）|
| 1 | overlay-13 `10BFh` | 同上，問**候選** |
| 7 | overlay-08 `021Ah` | 反應攻擊的否決（＝ `DS:2880h` 那四個，spec 059）|
| 0Ah（10）| overlay-24 entry 6 `0CF3h` | **攻擊者**身上會影響命中的效果 |
| 0Ch（12）| overlay-24 entry 7 `0DB2h` | **豁免**的修正（spec 075）|
| 10h（16）| overlay-24 entry 5 `0C81h`、entry 6 `0D00h` | **目標**身上會影響命中的效果 |
| 12h（18）| overlay-13 entry 1 `005Eh` | 每回合初始化時調整攻擊次數的工作值 `DS:6778h` |
| 6 | overlay-24 entry 19 `1351h` | 套用**傷害**時的修正（`DS:6776h`）|
| 4 | overlay-13 `021Eh` | 近戰**傷害骰**算完（含背刺倍數）之後的修正 |
| 0Eh（14）| overlay-09 `0B66h` | 怪物接近迴圈裡問的 |
| 11h（17）| overlay-09 `1172h`／`11C5h` | 士氣那一段 |
| 0Bh（11）| overlay-13 `1595h`、overlay-22 `09B2h` | **命中判定**的修正 |
| 0Fh（15）| overlay-08 `0289h`、`065Eh` | **輪到這個人行動**時跑一遍 |

### 群組 2／3 是兩種攻擊形態命中後的特殊攻擊（2026-09-13）

overlay-13 的攻擊區段以 `[bp-15h]` 保存攻擊形態 1／2；成功造成傷害後，
`1740h..174Dh` 取該值、加一，再呼叫 `0100h:002Fh`。因此形態 1 派發群組 2，
形態 2 派發群組 3。呼叫交給的是攻擊者記錄；效果處理常式再沿攻擊者 runtime
`+108h` 的目前目標指標找到受擊者。

`MONnSPC.DAX` 與兩支處理常式互相閉合：WIGHT／WRAITH 的節點帶 `55h`，
SPECTRE／VAMPIRE 帶 `56h`；overlay-12 entry 80 `2372h` 明確把常數 1 傳給
`211Eh`，entry 81 `2392h` 明確傳 2。兩碼同時列在群組 2 與 3，所以不論掛在
哪一種攻擊形態，成功命中後都吸取一級／兩級。完整來源雜湊、bytes 與樣本見
`docs/audit/dos-energy-drain-special-attacks.json`。

### 群組 11 是命中判定（2026-09-05）

兩個呼叫端是同一個型，連前置都一樣：

```
overlay-13 1587h  lcall 010A:0043(記錄)      ; 前置
         158Ch  mov al,0Bh / push ax
         1595h  lcall 0100:002F            ; ← 群組 11
         15AAh  push cs; call 2558h(攻擊者, 目標)

overlay-22 099Eh  lcall 010A:0043(記錄)      ; 同一個前置
         09B2h  lcall 0100:002F            ; ← 群組 11
                lcall 0100:003E(對手[+111h])  ; 內部 AC（spec 051）
```

overlay-22 那一支 spec 074 已經從另一頭寫過：那是**要碰觸才生效的五個法術**
（Cause Light Wounds、Shocking Grasp、Cause Blindness、Cause Disease、
Bestow Curse）先擲中的那一步，結果為零就把效果整個取消。overlay-13 是近戰
版本。兩邊都是「擲命中之前先問效果系統要加減多少」。

所以 `1Eh`（站在雲裡）落在群組 11 的意思是：**雲會改命中**。

### 群組 15 是「輪到這個人行動」（2026-09-05）

| 呼叫端 | 位置 |
|---|---|
| overlay-08 `0289h` | 回合開始那一段：清掉 `[+108h]` 的 `+0Fh`／`+12h`／`+07h`、問群組 7、把行動者存進 `DS:5CF0h`、`013D:008E`、`010A:0043`、`010A:0039`，然後問群組 15 |
| overlay-08 `065Eh` | **函式的第一道指令**——那支函式接著就用 `05BBh` 的字串常式把 `Move`／`View Aim`／`Use`／`Cast`／`Turn`／`Quick Done` 拼出來，是**戰術行動選單**的建立 |

兩個都在「這個人要動了」的當口。`1Eh` 同時落在群組 11 與 15，
對得上雲的兩種可見效果：行動時結算一次，出手時改命中。

**這一組的處理常式讀了兩支，兩支同形**（詳見 spec 121）：

| 代碼 | 進入點 | 訊息 | 額外做的事 |
|---|---|---|---|
| `15h` | overlay-12 entry 22 `0725h` | `is silenced` | 無 |
| `1Eh` | overlay-12 entry 29 `0A76h` | `is coughing`（＝臭雲）| 重畫、AC 每次變差 2 點（下限 AC 10）|

共同的骨架是：`記錄[+108h]` 的 `+2` 非零 → 印「<名字> <訊息>」＋
`010A:0084(1, 0Ah)` 停一下；然後把 `+2` 與 `+1` 都清成 0。
overlay-13 `1404h` 每回合把這兩個 byte 設回 1，**所以 `+1`／`+2` 就是
這一回合的行動權**，而 `+2` 兼作「還沒喊過話」——一回合只喊一次。

三個累加格因此也定了下來：

| 位址 | 是什麼 | 由誰擲 |
|---|---|---|
| `DS:6780h` | **命中骰**（1d20，自然 1 直接失手、自然 20 改寫成 100）| overlay-24 entry 5／6 |
| `DS:6774h` | **豁免骰**（1d20 加 `+101h` 與呼叫端修正）| overlay-24 entry 7（spec 075）|
| `DS:6788h` | 目前這一次的**豁免類別**，給效果常式判斷該不該加成 | 同上 |
| `DS:6776h` | **傷害量**。近戰由 overlay-13 `01CFh` 擲出來寫進去（背刺倍數在這之後乘，spec 050），套用時由 overlay-24 entry 19 再讀一次 | 兩處 |
| `DS:6777h` | 傷害訊息的旗標：低三位選句型（`13DEh` 的 `and 0F7h` 之後比 1／2），位元 3 另一個（`14ABh` 的 `and 8`）| 由施法的處理常式（overlay-22）設 |

所以「效果碼 `47h` 讓 `DS:6780h` 減 4」就是「這個效果讓對它出手的攻擊 −4」。

傷害那一支的順序值得記：**先讓群組 6 調整 `DS:6776h`，才套豁免**——
`1354h` 之後，豁免規則 1 是全免（傷害寫 0）、規則 2 是減半（整數除 2）。
所以「減半」減的是效果調整過的值，不是原始骰值。

## 群組 10／16：命中擲骰（overlay-24 entry 6，2026-09-26，#86）

輸入：overlay-12（SHA-256 `d1b05743…`）、overlay-24（`e878166e…`）、`START.EXE`
（`12811cbc…`，DS 檔案基準 30640，`DS:2880h` 讀到 `33 34 35 1F` 為正對照）；
`objdump -D -b binary -m i8086`（`coab-go-test:20260729`）。以下除註明者外皆 exact。

overlay-24 entry 6（`0CB5h`，`retf 0Ah`，參數：攻擊者 `[bp+0Ch]`、目標 `[bp+08h]`、
目標 AC `[bp+06h]`；呼叫端 overlay-13 `16D2h`、overlay-22 `09CEh`）：

```
0CBF  0FCCh(攻擊者)              ; 0FD8h 推 19h 查、0FF9h 用 entry 2 摘，迴圈到沒有
0CC9  6780h = 0DE5h(1, 14h)      ; entry 8，1d20
0CD6  80 3E 80 67 01 / 7E 7B     ; <= 1 → 落空（效果系統一支都不問）
0CDD  == 14h → 6780h = 64h
0CE9  B0 0A 50 … E8 EC F5        ; 02E2h(0Ah, 攻擊者)：群組 10
0CF6  B0 10 50 … E8 DF F5        ; 02E2h(10h, 目標)：群組 16
0D28  80 3E 80 67 00 / 7C 29     ; 有號小於 0 → 落空
0D2F  cbw(6780h) + 攻擊者 +110h + [4937h]+6E2h／6E0h（依 +10Eh）>= AC → 命中
```

群組 10 的記錄是攻擊者，群組 16 是目標。處理常式讀「目標」時走的是記錄 `+108h` 的 `+0Ah`
（寫進 `DS:6784h`），在這一次擲骰裡就是被打的那一個（strong inference：攻擊包裝在擲骰前
設好 `+0Ah`；反應攻擊暫時把它指向正在離開的人，spec 059）。

| 群組 | 碼 | 處理常式 | bytes | 對 `6780h` |
|---:|---|---|---|---|
| 10 | `01h` | entry 5 `010Fh` | `FE 06 80 67` | +1 |
| 10 | `02h` | entry 6 `0121h` | `FE 0E 80 67` | −1 |
| 10 | `21h` | entry 31 `0BBEh` | `80 2E 80 67 04` | −4；另把記錄 `+111h`／`+112h` 各減 4、`6774h` 減 4 |
| 10 | `24h` | entry 34 `0C2Dh` | `80 2E 80 67 04` | −4（`6774h` 也 −4）|
| 10 | `31h` | entry 46 `12C1h` | `24 10 … F7 F9` | 節點 `+3` 位元 4 等於攻擊者 `+10Eh` → `0C1Ch`（`FE 06 80 67`）+1，否則 −1 |
| 10 | `03h` | entry 7 `0141h` | `26 80 BD 9F 00 04` | 目標 `+9Fh == 4` → +2 |
| 10 | `06h` | entry 9 `01C9h` | `3D 0A 00`／`3D 09 00`／`3D 0C 00`／`3D 04 00` | 目標 `+9Fh` 0Ah +1、9／0Ch +2、4 +3 |
| 10 | `12h` | entry 20 `068Bh` | `81 C7 46 03` | 目標 `+9Fh == 1`、`+6Ch == 1`、名字是 `DS:0356h` 起四格之一 → +1 |
| 10 | `1Ah` | entry 26 `0956h` | `81 C7 86 03` | 同上，`DS:0396h` 起八格 |
| 16 | `19h` | entry 25 `0927h` | `80 2E 80 67 04` | −4（`677Ch` 只給 AI 挑目標）|
| 16 | `47h` | entry 66 `1737h` | `80 2E 80 67 04` | −4 |
| 16 | `25h` | entry 35 `0C40h` | `26 80 7D 03 00 / 7E 0A` | 目標 runtime `+3` 有號大於 0 → `FFh` |
| 16 | `2Fh` | entry 44 `1208h` | `81 C7 06 04` | `5CF0h` 的 `+9Fh == 1`、`+6Ch & 7Fh == 2`，**目標自己的 `+0Ah`** 名字是 `DS:0416h` 起五格之一 → −4 |
| 16 | `30h` | entry 45 `1283h` | `BF 75 12`／`BF 7D 12` | `5CF0h` 的 `+9Fh == 1`、名字是 BUGBEAR 或 GNOLL → −4 |
| 16 | `59h` | entry 83 `2461h` | `80 3E D7 6C 00`、`C6 06 80 67 FF`、`0C 10` | 相位 `6CD7h == 0` 且 `6780h == 0` → 節點 `+3 &= 0Fh`；否則位元 4 沒立 → `FFh` 並立起 |

名字表（string[15]，每格 16 bytes，比對用 RTL `05BBh:0724h`）：`DS:0356h` KOBOLD、
KOBOLD LEADER、GOBLIN、GOBLIN LEADER；`DS:0396h` ORC、ORC LEADER、GOBLIN、GOBLIN
LEADER、HOBGOBLIN、HOBGOBLIN CHIEF、GAGOOL、MACE（迴圈到 8，第 8 格照讀）；`DS:0416h`
OGRE、OGRE LEADER、TROLL、FIRE GIANT、HILL GIANT（第 6..8 格是別的資料）。

這幾個碼從哪來：overlay-16（建角）依種族 `+2Eh` 掛——矮人 `5Ah 61h 1Ah 2Fh`、侏儒
`61h 12h 2Fh 30h`、精靈 `6Bh`、半精靈 `7Ch`、半身人 `5Ah 61h`（`07A8h..0905h`，
`9A 52 00 00 01` 前推的碼）；祈禱術 `31h`、隱形術 `19h`、閃現術 `25h`、致盲 `21h`、
降咒 `24h` 是參數表 `+0Ah`；相位蜘蛛帶 `25h`、提拉尼薩克斯帶 `59h`（`MONnSPC.DAX`）。
所以 `12h`／`1Ah` 是矮人與侏儒對類人的 +1，`2Fh`／`30h` 是他們被大型對手打的 −4，
`59h` 是幻影移位的「第一擊必落空」。

remake：`gamepack.HitRollEffects.Apply` 照上表的順序跑兩組，`DropInvisibility` 是
`0FCCh`；`tacticalState.hitRollAfterEffects` 接進 `resolveAttackSwings`（一般攻擊與
反應攻擊同一支），`areaEffectNode` 是 `014Dh` 的作用範圍那一條（`combat.NearbyCells`，
朝向 0FFh、預算 6；串列順序取 roster 順序，strong inference）。測試從 `Update()` 按 A：
`TestAttackerEffectsAdjustTheHitRoll`、`TestPrayerAreaAdjustsBothSides`、
`TestTargetEffectsAdjustTheHitRoll`、`TestDisplacementMissesTheFirstAttack`、
`TestAttackingDropsInvisibility`，每一條都拿同一骰不帶效果的對照組比。

## 群組 9：掛效果前的免疫（entry 20，2026-09-26，#86）

entry 20（`1656h`）的 `166Fh..1687h`：`DS:6775h = 碼`、`02E2h(9, 目標)`、`6775h == 0`
就印 "is Unaffected"（`1648h`）不掛。十個處理常式全部經 overlay-12 `0000h` 動 `6775h`：

```
0000  f(碼)：碼 == 0 或 6775h == 碼 → 6776h = 0、6775h = 0          ; retf 2
2910  f(百分比)：x = 010Ah:00D4h(6779h)（施法者等級）
      門檻 = byte(百分比 − (11 − x) × 5)
      6775h != 0 或 6777h & 8 → 1d100（entry 8）<= 門檻（無號 ja）→ 0000h(0)
```

| 碼 | 處理常式 | 做什麼 |
|---|---|---|
| `69h` | entry 99 `296Eh` | `2910h(32h)`：魔法抗性 50% |
| `6Ah` | entry 100 `297Eh` | `2910h(0Fh)`：15%（施法者 7 級以下門檻繞成負數的無號值，一定抗掉）|
| `6Bh` | entry 101 `298Eh` | 1d100 <= 5Ah → 擋 `35h`、`0Bh`（精靈）|
| `6Ch` | entry 102 `29B4h` | 擋 `0Bh`、`35h` |
| `6Dh` | entry 103 `29CBh` | 擋 `34h` |
| `6Eh` | entry 104 `29DBh` | `6777h & 2` → 全擋 |
| `6Fh` | entry 105 `29F4h` | 擋 `37h`、`34h`；`6788h == 0` 時 `6774h = 64h` |
| `70h` | entry 106 `2A17h` | `6777h & 1` → 全擋 |
| `7Ch` | entry 118 `2DF9h` | 1d100 <= 1Eh → 擋 `0Bh`、`35h`（半精靈）|
| `7Dh` | entry 119 `2E1Fh` | 擋 `0Bh`、`35h`、`34h`、`37h`；`6774h` 同 `6Fh` |

`6777h` 在 `08BCh` 開頭依 `[bp+0Ch]`（傷害）寫：沒有傷害就寫 0（`08DBh`）。祝福、詛咒、
急速、緩速（`0FCBh`、`27DCh` 推四個 0）與魅惑（`1231h..1239h` 推 1、0、0）都是 0，所以
`6Eh`／`70h` 在這幾條路上不作用；定身術（overlay-22 `17B0h`）不經 `08BCh`，`6777h` 是
上一次留下的值——`08BCh` 收尾 `0A6Ah` 與 overlay-13 `0210h` 都寫 0，remake 當 0
（strong inference）。

remake：`gamepack.SpellEffectImmunity`；`app.unaffectedBySpellEffect` 接在三條會走
entry 20 的路上：模式 0Ah（`08BCh` `0A5Ah`）、魅惑（`124Ah` → `08BCh`）、定身術
（`17B0h`）。豁免照原版先擲（`096Bh`／`1740h`），群組 9 在 entry 20 裡才問，所以亂數
順序是 d20 再 d100。測試：`TestMagicResistanceBlocksTheCurse`、
`TestHoldImmunityCodesBlockTheHold`、`TestCharmImmunityCodesBlockTheCharm`。

## 群組 11／12／6：只掛效果那一批法術的碼（2026-09-26，#89）

輸入：overlay-12（`d1b05743…`）、overlay-13（`4d53df20…`）、overlay-22（`967065cc…`）、
overlay-24（`e878166e…`）、overlay-25（`9fede24b…`）；`objdump -D -b binary -m i8086`
（`coab-go-test:20260729`）。以下除註明者外皆 exact。

**群組 11 每一次都從重算過的 AC 起算。** 兩個呼叫端（近戰 overlay-13 `1587h..1595h`、碰觸法術
overlay-22 `09A4h..09B2h`）都先呼叫 `010Ah:0043h`（overlay-25 entry 7 `0BBEh`，整份重算戰鬥數值，
spec 063）再派發群組 11，然後才擲命中。所以群組 11 對 `+111h` 的改動每一次出手只作用一次、
不累加。群組 11 的處理常式寫進 `DS:6780h`（命中骰）與 `DS:6774h`（豁免骰）的值在這個時點
**看不到**：entry 6 在 `0CC9h` 重擲 `6780h`、entry 7 在 `0D71h` 重擲 `6774h`。

**群組 12 在豁免骰算好之後、比目標值之前。** entry 7（`0D61h`）：`0D71h` 擲 1d20，1 失敗、
20 成功直接返回；`0DA5h` `6774h = 骰 + +101h + 修正`（`mov ds:6774h, al`，byte）、`0DACh`
`6788h = 類別`、`0DB2h` 群組 12、`0DC8h` `26 8A 45 6D / 3A 06 74 67 / 77 06`——目標值**無號**大於
`6774h` 就失敗。

| 碼 | 常式 | bytes | 群組 11（AC）| 群組 12（豁免）| 群組 6（法術傷害）|
|---|---|---|---|---|---|
| `11h` 護盾 | entry 19 `065Eh` | `26 80 BD 11 01 39 / 73 09 / 26 C6 85 11 01 39`、`FE 06 74 67`、`80 3E 79 67 0F / 75 05 / C6 06 76 67 00` | `+111h` 墊到 39h | +1 | `DS:6779h`（目前法術）是 0Fh 魔法飛彈 → 0 |
| `21h` 失明 | entry 31 `0BBEh` | `26 80 AD 11 01 04`、`26 80 AD 12 01 04`、`80 2E 74 67 04` | `+111h`、`+112h` −4 | −4 | — |
| `24h` 降咒 | entry 34 `0C2Dh` | `80 2E 74 67 04` | — | −4 | — |
| `31h` 祈禱 | entry 46 `12C1h` → `0C1Ch` | `24 10 … F7 F9 / 3A 46 FF / 75 16`、`FE 06 74 67`／`FE 0E 74 67` | — | 節點 `+3` 位元 4 等於自己的 `+10Eh` +1，否則 −1（`014Dh` 半徑 6）| — |

remake：`gamepack.HitCheckArmourClass`（群組 11，近戰 `resolveAttackSwings` 與碰觸
`touchSpellHits` 共用 `tacticalState.hitCheckArmourClass`）、`gamepack.SaveRollAfterEffects`
（群組 12，`savedAgainstCategory`）、`gamepack.SpellDamageAfterEffects`（群組 6，`damageAfterSave`
在擲過豁免之後、套規則之前）。三張群組表裡其餘的碼沒有接，見 OPEN。

## 群組 12／6／4／5：#96／#99 接上的碼（2026-09-26）

輸入同上一節；另加 overlay-13（`4d53df20…`）。以下除註明者外皆 exact（位元組逐條讀）。處理常式
的記錄是 `[bp+0Ch]`、節點 `[bp+08h]`、模式 `[bp+06h]`。

**entry 7 的豁免骰是 byte、無號比較。** `0DA5h` `A2 74 67`（`mov ds:6774h, al`）把
`骰 + [+101h] + 修正` 存成一個 byte，群組 12 的常式也全是 byte 加減（`80 06 74 67 imm`、
`80 2E 74 67 imm`、`FE 06`／`FE 0E 74 67`），`0DC8h` `26 8A 45 6D / 3A 06 74 67 / 77 06` 是
「目標值無號大於它就失敗」。所以算出負值時繞成 `0F0h` 以上、一定成功（自然 1 在 `0D79h` 就先失敗了，
最小的骰是 2）。remake 的 `savedAgainstCategory` 照這個比（原本的 hypothesis 由 bytes 定案）。

| 碼 | 常式 | bytes | 群組 12（豁免）| 群組 6（法術傷害）|
|---|---|---|---|---|
| `08h`／`2Dh` | entry 11 `0377h` | `C4 3E F0 5C / 26 80 BD A0 00 02`（`05`、`08`）、`80 06 74 67 02` | `DS:5CF0h`（輪到行動的人，施法時就是施法者）的 `+0A0h` 是 2／5／8 → +2 | — |
| `09h`／`2Eh` | entry 12 `03AEh` | 同形，比 0／3／6 | +2 | — |
| `0Ah` | entry 13 `03E5h` | `A0 77 67 / 24 02`、`A0 76 67 / 30 E4 / 99 / B9 02 00 / F7 F9 / A2 76 67`、`80 06 74 67 03` | `6777h & 2` → +3 | 同條件傷害減半（byte 零延伸除 2）|
| `14h` | entry 21 `06F4h` | 同形，`24 01` | `6777h & 1` → +3 | 同上 |
| `5Ah` | entry 84 `24ACh` | `80 3E 88 67 00 / 75 68`、`26 8A 45 14`、`3D 04 00 … 3D 14 00`、`A0 74 67 … A2 74 67` | 類別 0（毒）才加：體質 4..6 +1、7..10 +2、11..13 +3、14..17 +4、18..20 +5 | — |
| `61h` | entry 90 `2673h` | `80 3E 88 67 04 / 74 07 / 80 3E 88 67 02 / 75 68`，其餘同 `5Ah` | 類別 4（法術）與 2（魔杖）才加，同一張體質表 | — |
| `1Ch` | entry 27 `09CDh` | 見下 | — | 見下 |

- `+0A0h` 是陣營，值的順序與 creation.Alignments 相同（嚴守善良 0 … 混亂邪惡 8）。正對照：
  MON1CHA.DAX 的 GOBLIN／ORC／HOBGOBLIN 是 2（嚴守邪惡）、OGRE 與 VAMPIRE 是 8、GIANT SKELETON 是 4，
  與 AD&D 怪物圖鑑一致（strong inference 升為欄位語意；判斷式本身 exact）。`08h`／`09h` 在群組 11
  的 `80 2E 80 67 02`（命中 −2）寫在 entry 6 `0CC9h` 重擲之前，看不到；群組 10／16 沒有這兩個碼，
  所以 DOS 版的防護邪惡／善良**只改豁免**。
- `5Ah`／`61h` 的體質是記錄 `+14h`（能力值依序 `+10h..+15h` 是力量、智力、智慧、敏捷、體質、魅力；
  OGRE 的 `+10h` 是 18）。體質不在 4..20 時 `[bp-1]` 沒有寫，加上去的是堆疊殘值（unknown）；
  remake 不加。建角下限是 3，只有 3 會落在這裡。
- `DS:6777h` 由各傷害處理常式推給 `08BCh` 的第五個參數寫入（`08E2h`，傷害為 0 時 `08DBh` 寫 0）：
  致輕傷 `10ABh` 8、燃燒之手 `1192h` 9、魔法飛彈 `146Bh` 8、電擊之握 `14ECh` 0Ch、火球 `270Ah` 9、
  65 號 `302Fh` 8；射線 `287Ch` 在 `28DCh` 自己寫 0Ch。全部 overlay 掃 `C6 06 77 67`／`A2 77 67`
  的寫入（另有 overlay-12 `0227h` 9、`1A38h` 9、`1A6Dh` 10h、`30F7h` 8，都是怪物特殊攻擊的常式），
  **沒有一處立位元 1**：抗寒 `0Ah` 在 DOS 版的已知傷害上都不作用（exact：寫入點窮舉）。
- 群組 12 在豁免那一刻，`0Ah`／`14h` 改的 `6776h` 會被 entry 19 `1344h` 重寫，只有 +3 算數；
  群組 6 在 entry 19 裡，只有減半算數（豁免早就擲完）。

**`1Ch`（鏡影，`09CDh..0A47h`）同時在群組 5 與 6：**

```
09D3  Roll(1, 節點 +3 + 1)           ; entry 8，一定先擲
09E6  3C 01 / 76 5A                   ; <= 1 → 打到本尊
09EA  80 3E 79 67 00 / 76 53          ; DS:6779h == 0（不是法術）→ 返回
09F1  80 3E 7E 67 00 / 75 4C          ; DS:677Eh != 0（以一點收的範圍）→ 返回
09F8  push 0 / call 0000h             ; 6776h = 0
0A0F  "lost an image"、停一下
0A22  26 FE 4D 03                     ; 影像數減一；0A29h 為 0 → entry 2 摘掉
```

`DS:6779h` 只有法術派發（overlay-22 `0E95h` 寫、`0EA7h` 清）與 overlay-12 `1F7Eh` 寫（byte 掃描
窮舉），所以**群組 5（近戰，overlay-13 `022Ch`）的鏡影擋不下任何一擊，只會多擲一次骰**（strong
inference）；群組 6 擋的是單一目標的傷害法術。`DS:677Eh` 由 overlay-13 `20AEh` 在以一點收表時立起
（`21EEh`、`22ADh`；`20C0h` 先清），火球 `2634h`、射線 `2987h`（`2919h` 拉線之前）自己也立，
`0EACh` 派發之後清——所以火球與閃電束的後段擋不下，射線打瞄準那一格的第一下（`287Ch`，編號 60
的模式 4 不立）擋得下。

**群組 4／5 是近戰傷害骰之後的那兩次派發**（overlay-13 `0191h`，`retf 0Ah`：攻擊者
`[bp+0Ch]`、目標 `[bp+08h]`、形態 `[bp+06h]`）：`01CFh` 擲傷害進 `6776h`、背刺倍數、`0210h`
`6777h = 0`、`021Eh` 群組 4（攻擊者）、`022Ch` 群組 5（目標），然後 `1732h` 由 `02FEh` 扣血——近戰
不經 entry 19，沒有群組 6。`1Dh`（衰弱射線，entry 28 `0A4Ah`）：`A0 76 67 / 30 E4 / 99 / B9 04 00 /
F7 F9 / 8B D0 / A0 76 67 / 30 E4 / 2B C2 / A2 76 67`，傷害減掉自己的四分之一（byte 整數除法）。

remake：`gamepack.SaveRollEffects`（群組 12，照呼叫順序；`savedAgainstCategory` 拿回來的 byte 做
無號比較）、`gamepack.SpellDamageEffects`（群組 6：`0Ah 14h 11h 1Ch`）、`gamepack.MirrorImageAbsorbs`、
`gamepack.MeleeDamageAfterAttackerEffects`（群組 4）；盤面的 `+14h`／`+0A0h` 在開打時記下
（`rememberSaveRecord`：怪物與 NPC 讀記錄，隊員讀能力值與建角陣營），`DS:6777h`／`677Eh`／`6779h`
是 `tacticalState.SpellDamage`，castSpell 施完歸零。近戰在 `resolveAttackSwings` 擲完傷害骰之後
走 `meleeDamageAfterEffects`；射線每一格照 `287Ch` 的順序（豁免 → 群組 6 → 減半）。
送鍵測試（`cmd/pool-game/save_damage_effects_test.go`）：`TestDwarfOutsavesAHumanOnTheSameRoll`、
`TestProtectionRaisesTheSaveOnlyAgainstTheRightAlignment`、`TestResistFireHalvesBurningHands`、
`TestMirrorImageAbsorbsASpellButNotAMeleeHit`、`TestEnfeeblementCutsTheFoesMeleeDamage`、
`TestNegativeSaveRollWrapsIntoASuccess`；每一條都做過變異檢查（拿掉那一段規則就紅，2026-09-26 實跑）。

這一段順帶閉合了上一版 OPEN 的「`21h` 把 `+111h`／`+112h` 各減 4，要等 `+111h` 何時重算讀出來」：
重算就在群組 11 前面，所以對「被打」而言是每一次 −4、不累加（remake 照這個接）。在群組 12 的
時點改掉的 `+111h` 會留到下一次重算，而下一次被打之前一定先重算，所以看不到。

## 群組 17：士氣的讀取端

overlay-09 `1169h..1172h`（`B0 11 50 … 9A 2F 00 00 01`）與 `11BCh..11C5h` 兩次派發，
各在比較之前讓 `01h` 加 5、`02h` 減 5（不低於 0）調 `DS:6783h`；逐條讀在 spec 096
〈entry 8〉，remake 是 `gamepack.AdjustMorale`（#74）。送鍵測試
`TestBlessAndCurseMoveTheMoraleCheck` 用 `foeTurn` 驗兩個方向。

## 一百一十五支處理常式的覆蓋台帳

`docs/audit/dos-effect-handlers.json` 是機器產生的盤點：138 個在 overlay-12 的
代碼對到 **115 支相異處理常式**（有些代碼共用一支），另外 `49h`／`4Ah` 在
overlay-13、`58h` 由 overlay-22 填。每一支記了大小、指令數、寫了哪些全域、
碰了記錄的哪些欄位、呼叫了哪些跨 overlay 進入點。

幾個一眼看得出來的分群：

- **十二個代碼指到空常式**（entry 126／18／49／50，9–12 bytes，`retf 0Ah`
  什麼都不做）。它們在群組表裡出現，但永遠不會改變任何東西。
- **四支會立起 `DS:677Ch`**（否決）：entry 25 ＝ `19h`、35 ＝ `25h`、
  66 ＝ `47h`、120 ＝ `7Eh`。
- **依代碼數計的全域寫入**：`6780h`（命中骰）20 個、`6776h` 19 個、
  `6774h`（豁免骰）16 個、`6784h`／`6786h`（目前目標的遠指標）13 個、
  `6777h` 7 個、`677Ch` 4 個。
- 最大的五支是 entry 37（`28h`，728 bytes）、76（`53h`）、77（`54h`）、
  122（`83h`）、68（`4Bh`）。

## 效果的生命週期

七支撐起整個系統：

| 位置 | 做什麼 |
|---|---|
| overlay-24 entry 10（`0E54h`）| **掛上**一個節點：`GetMem(9)`，接在記錄 `+7Fh` 串列的**尾端** |
| overlay-24 entry 2（`0028h`）| **摘掉**並 `FreeMem(9)`；`+4` 非 0 時先以模式 1 叫一次處理常式 |
| overlay-24 entry 1（`0000h`）| 用代碼查 `DS:678Ah` 的表，分派給那個代碼自己的處理常式 |
| overlay-24 entry 15（`107Bh`，`0100h:006Bh`）| 身上有這個碼就印 "is Cured"、摘掉最早的那一個、回 1（治盲、解病、急速／緩速互相抵銷都走它）|
| overlay-24 entry 20（`1656h`，`0100h:0084h`）| 法術掛效果：先問群組 9（免疫）、再看豁免；同碼舊節點比較短或新的不計時就摘，然後一律用 entry 10 掛新的（spec 098）|
| overlay-24 entry 3（`02E2h`）| 群組 → 一串代碼 |
| overlay-24 `014Dh` | 這個代碼影響不影響得到這筆記錄 |

節點是 **9 bytes**，欄位由 entry 10 的參數填：

```
0e54  f(記錄: far; 代碼: byte; 持續: word; 等級: byte; 有收尾: byte)   retf 0Ch
+0     代碼
+1..+2 持續（word）
+3     下效果者的等級（低四位；0FFh 代表解除魔法解不掉，spec 098）
+4     「摘掉時要叫一次處理常式」旗標
+5..+8 下一個節點的遠指標
```

掛上去是**接在尾端**不是插在頭：`0E98h` 沿 `+5` 走到 NULL 才接。所以效果的
順序就是掛上去的順序，而 `21DCh`（spec 059）的線性搜尋找到的是**最早掛上**
的那一個。

### `0Ch`／`26h` 的 `+3` 是可逆力量快照

這兩個代碼共用 overlay-12 entry 15（`04CBh..05E0h`），節點 `+3` 不放一般的
施法者等級。overlay-24 entry 16／17（`10DEh`／`1107h`）定義其編碼：力量為
18 時存「百分位＋1」，其他力量存「力量＋100」；最高位代表這個節點目前
inactive。原始 bytes、輸入雜湊與 IDA 版本見
[`docs/audit/ida-strength-effect-teardown.json`](../audit/ida-strength-effect-teardown.json)。

entry 18（`1158h`）套用較強的新效果時，會把原 active 節點改存它自己的力量並
立起最高位，新 active 節點則保存共同基礎值。較弱效果也會掛節點，但一開始就是
inactive。active 節點到期時，entry 15 先還原基礎值，再掃其餘 `0Ch`／`26h`：
若還有別的力量效果，就挑最強者、清掉它的 inactive 高位並改由它生效。因此效果
可按任意順序到期，不會提早丟掉仍有效的較強／較弱增益。

remake 的 `gamepack.ApplyStrengthEffect`／`ExpireStrengthEffect` 保存這份契約；
地圖 `advancePartyEffects` 與戰場 `tickEffects` 共用 `expiredEffectTeardown` 回寫
角色與角色庫。玩家路徑測試從 `Update()` 送 `C`、選法術、選自己，確認節點掛上，
再推完原版參數表的十回合並確認力量由 18 回到 10。整體覆蓋分類見
[`docs/audit/effect-teardown-coverage.json`](../audit/effect-teardown-coverage.json)。

**21 個代碼會呼叫 entry 10**——那些效果會替自己掛節點；**15 個會呼叫
entry 2**——那些會把自己（或別的代碼）摘掉。25 個只呼叫 RTL 的字串常式，
也就是只印一句話。

## 節點 `+3` 那個 byte 打包了四件事

代碼 `0Bh`（**魅惑**，overlay-12 entry 14 `040Ah`）把它讀得很清楚：

| 位元 | 意思 |
|---|---|
| 0..3 | 下效果者的等級（`0FFh` 代表解除魔法解不掉，spec 098）|
| 5（`20h`）| 這個效果已經套過了 |
| 6（`40h`）| 被魅惑**之前**原本的陣營 |
| 7（`80h`）| **施法者**的陣營 |

```
040a  f(模式: byte; 節點: far; 記錄: far)
040d  模式 != 0（摘掉時的收尾）：
0416    陣營 = 節點 +3 的位元 6
0427    記錄 +10Eh = 陣營                  ; 還原
042f    +84h == 0B3h → +84h = 0            ; 士氣值還原
0443  模式 == 0（套用）：
0446    節點 +3 的位元 5 已立 → 返回        ; 套過了就不重複
0450    節點 +3 += 20h + (記錄目前的 +10Eh << 6)   ; 記下原陣營並立旗標
047c    記錄 +10Eh = 節點 +3 的位元 7       ; **倒戈到施法者那一邊**
048f    記錄 +10Fh = 1                      ; 改由 AI 分派
0498    +84h <= 7Fh → +84h = 0B3h           ; 給它一個士氣值
```

**與施法端逐位元對上**：Charm Person（spell `0Ah`，overlay-22 `11C5h`）推給
`08BCh` 的等級覆寫是 `(施法者 +10Eh << 7) + 施法者等級`（spec 098）——
位元 7 是施法者的陣營、低位是等級，正是這裡讀的兩件事。兩份各自解出來的
東西吻合。

`0B3h` 這個士氣值也不是隨手挑的：位元 7 立起代表「做士氣判定」，低七位 `33h`
× 2 ＝ 102 ＝ `66h`，剛好落在 spec 096 那個「超過 102 就當作不會逃」的邊界上。

## 四十八個代碼的修正算式

處理常式對累加格的改動可以機械抽出來（`ds:<格>, <值>` 這個形狀）。
115 支裡有 **48 個代碼**直接改累加格，逐條如下。其餘的做別的事——換句話說，
「還沒讀」的範圍已經從 115 支縮到「不直接改修正的那些」。

| 代碼 | 對命中骰 | 對豁免骰 | 對傷害 | 其他 |
|---|---|---|---|---|
| `01h` | +1 | | | `DS:6783h` +5（祝福，群組 10／17）|
| `02h` | −1 | | | `DS:6783h` 小於 5 清 0、否則 −5（詛咒，群組 10／17）|
| `03h` | +2（目標不死）| | +2 | |
| `06h` | +N（依目標種類）| | +N | 傷害訊息 = 9 |
| `08h`／`09h` | −2 | +2 | | |
| `0Ah`／`14h` | | +3 | 覆寫 | |
| `11h` | | | **歸零** | |
| `19h`／`47h` | −4 | | | 否決（`DS:677Ch`）|
| `21h`／`24h` | −4 | −4 | | |
| `25h` | **FFh**（自動落空）| | | 否決 |
| `2Dh`／`2Eh` | −2 | +2 | | |
| `2Fh`／`30h` | −4 | | | |
| `3Dh` | | +4 | −2 再覆寫 | |
| `59h` | **FFh**（自動落空）| | | |
| `5Ah`／`61h` | | 覆寫 | | |
| `6Fh`／`7Dh` | | **= 100**（自動成功）| | |
| `71h`／`72h`／`76h`／`5Dh` | | | 覆寫 | |
| `7Ah` | | | 覆寫再加 | |
| `7Eh` | | | | 否決 |

**與 spec 115 對得上**：`21h` 是神殿治得好的**失明**、`24h` 是**詛咒**，
而它們的效果都是「命中 −4、豁免 −4」。兩份各自解出來的東西吻合，
所以這張表的讀法是對的。

祝福術掛的是 `01h`、詛咒術 `02h`（參數表 `+0Ah`，spec 074）：overlay-12 entry 5 `010Fh`
`80 06 83 67 05 / FE 06 80 67` 同時改兩格——命中骰 `DS:6780h` 加 1、`DS:6783h` 加 5；entry 6
`0121h` 反過來（`DS:6783h` 夾在 0 以上）。兩碼都在群組 10（攻擊者的命中）與群組 17（士氣那一段），
呼叫端只讀自己關心的那一格，所以祝福是「出手命中 +1」加上群組 17 的 +5
（spec 098〈模式 0Ah：效果怎麼掛上去〉，#81）。

群組 18 的三個碼（2026-09-26，#81，exact）：

| 碼 | 處理常式 | 做什麼 |
|---|---|---|
| `27h` 急速 | entry 36 `0C67h` | 節點 `+3` 位元 4 沒立就立起、印 "ages"、記錄 `+30h`（年齡）加一；`DS:6778h` 左移一位 |
| `2Ah` 緩速 | entry 41 `10CAh` | `DS:6778h` 除以 2 |
| `3Ah` | entry 53 `145Ah` | runtime `+6`（移動）清 0；`DS:677Bh` 非 0（算移動那一次）時 `DS:6778h` 清 0 |

`DS:6778h` 是回合初始化（overlay-13 entry 1）裡攻擊次數編碼與移動預算輪流借用的工作值，
`DS:677Bh` 區分兩者（0 是攻擊、1 是移動）。

一支常式可以同時碰好幾個累加格——它不知道是誰叫它的，而呼叫端只會讀自己
關心的那一格，所以無關的寫入沒有副作用。

## 誰在問哪一組

| 呼叫端 | 群組 |
|---|---:|
| overlay-08 `021Ah` | 7 |
| overlay-08 `0289h`、`065Eh` | 15 |
| overlay-08 `08A3h` | 19 |
| overlay-09 `0B66h` | 14 |
| overlay-09 `1172h`、`11C5h` | 17 |
| overlay-10 `2019h` | 8 |
| overlay-12 `00D0h` | 13 |
| overlay-13 `005Eh`、`0178h`、`0DBBh` | 18 |
| overlay-13 `021Eh` | 4 |
| overlay-13 `022Ch` | 5 |
| overlay-13 `0603h` | 13 |
| overlay-13 `10BFh` | 1 |
| overlay-13 `10FDh` | 0 |
| overlay-13 `1595h` | 11 |
| overlay-13 `174Dh` | 推的是變數，不是常數 |
| overlay-22 `09B2h` | 11 |
| overlay-22 `2835h` | 18 |

**掃描面有洞，不要當成「這些群組沒人用」**：這張表只認得
`mov al, imm8; push ax; …; call far 0100h:002Fh` 這一種形狀，`174Dh` 就是一個
推變數的反例（`8A 46 EB 30 E4 40 50`＝`al = [bp-15h] + 1` 再推）。
群組 2、3、6、9、10、12、16 沒有出現在表裡，可能是被變數形式
的呼叫用掉的。`START.EXE` 也掃過，常駐段一次都沒有呼叫這一支。

只有 `174Dh` 一個是真的變數。overlay-22 `2835h` 乍看也像——它推的是
`[di+6B87h]`／`[di+6B85h]`——但**變數的是記錄指標不是群組**：群組在前面
`281Fh` 的 `B0 12 50`，是常數 18。兩者不要混。

## OPEN

- 115 支處理常式裡逐條讀過內容的（entry 25／35／66／120／126、
  overlay-12 entry 1、entry 22＝代碼 `15h`、entry 29＝代碼 `1Eh`，#81 的
  entry 5／6／36／41／53＝代碼 `01h`／`02h`／`27h`／`2Ah`／`3Ah`，#86 的
  entry 7／9／20／26／31／34／44／45／46／83／99..106／118／119，#89 的
  entry 11／12／13／16／19／21／28＝代碼 `08h 2Dh`／`09h 2Eh`／`0Ah`／`0Eh`／`11h`／`14h`／`1Dh`，
  entry 17／23／27／32／71 的形狀，#96／#99 的 entry 17／23／24／27／28／32／40／42／43／71／84／90＝
  代碼 `0Fh`／`16h`／`17h`／`1Ch`／`1Dh`／`22h`／`29h`／`2Bh`／`2Ch`／`4Eh`／`5Ah`／`61h`，見 spec 098
  〈#99：收尾〉）。其餘的已經有機器盤點
  （大小、碰哪些欄位、呼叫誰），但語意還沒逐支閉合。
- `21h` 在群組 10（出手的人，`+111h`／`+112h` 的改動在那個時點沒有讀者）與 12（豁免）的
  `+112h` 用途還沒讀；群組 11 的 AC −4 已接（上面〈群組 11／12／6〉）。
- 群組 11 的 `1Eh`（雲）：原版每次出手前重算再 −2（`0A76h`），remake 由臭雲術的每回合結算直接
  改 `ArmorClass`（spec 121），兩者不同，未對齊。
- 群組 12 的 `3Dh 6Fh 7Dh` 不是法術或種族掛的（怪物特殊能力），沒有接。
- 群組 6 其餘的碼（`71h 3Dh 7Ah 3Ch 5Bh 69h 6Ah 70h 72h 76h 5Dh 65h`）沒有接。
- 群組 5 的 `29h`（防護普通飛彈，entry 40 `108Dh`）沒有接：`1028h(DS:5CF0h)` 取攻擊者的彈藥
  （`010Ah:0101h`，overlay-25 entry 45，還沒讀）或手上的武器（`+0CCh`），那一件的 `+32h` 為 0 時
  `0FB7h(目標, 64h)`：攻擊者有 `+0CCh`、距離（`010Ah:00C5h`）大於 1、Roll(1, 100) <= 100 → 印訊息、
  `6776h = 0`、`6780h = FFh`、`6781h` 減一。卡點：彈藥那一支沒讀，remake 的怪物也沒有 `+0CCh`
  那一格（手上的武器）；`6781h` 的語意未對。群組 5 其餘的碼也沒有接。
- `2Fh` 比的是目標自己 runtime `+0Ah` 的名字，remake 沒有隊員的那一格（只記敵方 AI 的
  `FoeTargets`），未接。
- 群組 10 的 `03h`／`06h` 同時加傷害格 `6776h`，但近戰傷害在命中之後才擲（`01CFh`），
  真正生效的是群組 4（`1Dh 03h 06h`，overlay-13 `021Eh`）；remake 的群組 4 只接了 `1Dh`。
- overlay-24 entry 5（`0C4Dh`，只問群組 16）的呼叫端還沒列。
- overlay-13 `1726h` 那一處讀 `DS:6776h` 的用途（`01CFh` 那一處已經確認是近戰
  傷害骰，spec 050）。
- 代碼的編號與法術編號**不是同一套**：`33h` 在法術表是閃電束（spec 069），
  在這裡是反應攻擊否決的四個之一。兩套編號要分開看。
- 群組編號的語意：0、1、4、6、7、9、10、11、12、14、15、16、17、18 有名字了
  （見上面兩張表），其餘還只有代碼清單。
