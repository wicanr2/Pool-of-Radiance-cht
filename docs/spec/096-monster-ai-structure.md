# Spec 096：怪物 AI 的骨架（overlay-09）

狀態：DRAFT（每回合的行為鏈、戰術模式、接近用的候選搜尋、entry 11 的判定、
entry 7 的玩家打斷、entry 8 的士氣與 entry 3 的用物品已讀出；entry 2 只讀到
入口）。
日期：2026-09-03。

## 為什麼要讀它

remake 目前的敵方回合是權宜作法：對目標格做一次 BFS，挑**繞得過去的實際
步數**最小的方向。這只讓「繞得過去」這件事成立，選路規則本身仍不是原版的
——原版是先列出目標周圍可站的格子再挑（見下面的 overlay-31 entry 6）。

會走到 BFS 是因為更早的版本用直線距離挑方向，在**斜向、牆很多的盤面上人
一多就互相擋死**：41 隻敵人對 6 人隊伍、隊伍完全不還手，打到第 852 回合
我方仍是滿血，敵方回報 `FOE 31 CLOSED 0 STEPS ON 6`。四個可能的成因都
排除掉了（見 WORKLIST），剩下的就是移動規則。

## 入口與分派

overlay-08 entry 3（`01E4h`）依角色記錄的 `+10Fh` 分派：非零走
overlay-09 entry 1（`000Fh`），零走 overlay-08 `0307h` 的玩家指令迴圈。

## overlay-09 entry 1：一隻怪物的一個回合

它是一條**「依序試各種行為，誰先動就停」**的鏈：

```
0015h  acted = entry 7（0FC8h）
0027h  overlay-25 entry 21（1830h）
002Ch  記錄 +10Dh 為 0 時：acted = overlay-25 entry 34（266Dh）
0045h  mode = runtime 子結構的 +15h
0054h  mode 為 0 或大於 4 時重擲：
         骰(1,4) 不是 1 就沿用舊的；
         否則 mode = 骰(1,8)，等於 8 再改成 骰(1,2) + 4，其餘 骰(1,4)
00A6h  寫回 +15h
00B5h  還沒動 → acted = entry 8（10DDh）
010Fh  還沒動 → entry 3（03E3h）成立時交給 overlay-25 entry 34
012Eh  runtime +0 非零 → overlay-22 entry 5（0C14h）再交給 entry 34
0169h  還沒動 → entry 2（0203h）
```

所以**每隻怪物身上帶一個戰術模式**（runtime `+15h`，值 1..6），
多數回合沿用，四分之一的回合重擲；重擲時 1..4 佔八分之六、5..6 佔八分之二。

entry 2（`0203h`）是施法那一支：runtime `+11h` 為 0 且記錄 `+96h`（牧師等級）
大於 0 時，問 overlay-13 entry 13（`1352h`）有沒有法術可放。

## 接近用的候選搜尋：`0912h` 早就解完了

entry 11（`0255h`）呼叫的 `0138h:003Eh` 是 **overlay-31 entry 6（`0912h`）**，
而那一支在 **spec 056 已經逐條閉合，也已經實作成
`internal/combat.NearbyCells`**：展開雙方佔格、過朝向弧、走 `TraceMovement`
取最小成本、朝向未指定時自 0 起找第一個成立的方向。

它前半用的 `013Dh:002Ah` 是 overlay-32 的 `0000h`：

```
000a  [bp+10h]（體型類別）為 0 → 回 false
0012  di = 體型 × 8 + 索引 × 2
0028  *[bp+0Ah] = DS:[2858h + di]      ; dx
0048  *[bp+06h] = DS:[2859h + di]      ; dy
0052  dx 為負 → 回 false
```

那張表就是 spec 056 的**佔格偏移表**（折疊常數 `2858h`、實際起點 `DS:2860h`、
每列 8 bytes、`0FFh` 代表該列到此為止）：體型 1 一格、2 直向兩格、
3 橫向兩格、4 是 2×2。原始位元組重新量過一次，逐位元組相符。

所以「原版先列出目標周圍可站的格子再挑」這件事，**共用機制本身不缺**，
缺的是敵方回合怎麼用它。

### entry 11（`0255h`）是「這一格旁邊有沒有自己人」

```
025Fh  push X、Y
0272h  push 型別表[型別 × 16 + 31A3h]        ; 體型類別
0279h  push 0FFh                              ; 朝向不指定
027Bh  push 1                                 ; 成本預算 1 ＝ 貼身
027Fh  push DS:6674h                          ; 結果表
0287h  call 0138h:003Eh                       ; 0912h
028Ch  n = DS:6678h
02A1h  side = overlay-25 entry 30（23F5h）(DS:5CF0h)
02BCh  逐筆取 DS:6676h + i × 3 的 combatant 索引，查 DS:6517h 拿記錄
02CCh  記錄 +10Eh 不等於 side → 回 true
```

`23F5h` 整支只有一件事：`f(記錄) = (記錄[+10Eh] == 0) ? 1 : 0`，也就是
**那一方的對立陣營值**。所以「`+10Eh` 不等於 `23F5h(自己)`」＝「跟自己同一邊」。
`0255h` 回的是「以這個體型站在 (X, Y) 時，貼身範圍內有沒有自己人」。

型別表 `31A3h + 型別 × 16` 的 `+0` 是體型類別；同一張表的 `+1`
（`31A1h` 那一欄）與 `+2` 由 `02EAh` 那一支讀，語意還沒定。

## overlay-32 entry 2 就是佔格展開（spec 056 已有）

`0912h` 為 `i = 0..3` 呼叫的 `013Dh:002Ah` 是 overlay-32 的 `0000h`：

```
000a  [bp+10h]（體型類別）為 0 → 回 false
0012  di = 體型 × 8 + 索引 × 2
0028  *[bp+0Ah] = DS:[2858h + di]      ; dx
0048  *[bp+06h] = DS:[2859h + di]      ; dy
0052  dx 為負 → 回 false
```

那張表就是 spec 056 已經閉合的**佔格偏移表**（折疊常數 `2858h`、實際起點
`DS:2860h`、每列 8 bytes、`0FFh` 代表該列到此為止）：體型 1 是一格、
2 是直向兩格、3 是橫向兩格、4 是 2×2。原始位元組重新量過一次，逐位元組相符。

所以 `0912h` 的前半是「把目標的體型展開成最多四格」，不是四個方向。

## entry 7（`0FC8h`）是玩家打斷敵方回合

它不是行為，是**每一隻怪物動之前先看玩家有沒有按鍵**：

```
0fd2  0512h:02FAh()        ; 有按鍵嗎？沒有就回 0
0fde  026Bh:0048h()        ; 讀鍵碼；回 0 就再讀一次（擴充鍵的第二個位元組）
0ff4  鍵 == 32h（'2'）→ 把 DS:6D23h 反相，印 "Magic On" 或 "Magic Off"
103e  鍵 == 20h（空白）→ 走 DS:5CF4h 的整個串列，把 +84h < 80h
                          而且 +10Ch != 1 的記錄的 +10Fh 清成 0
```

兩段字就內嵌在同一顆 overlay：`0FB5h` 是 `Magic On`、`0FBEh` 是 `Magic Off`。
所以：

- **`DS:6D23h` 是「電腦幫我放法術」的開關**，戰鬥中按 `2` 切換。
- **空白鍵把控制權交回玩家**：`+10Fh` 就是 overlay-08 entry 3 拿來分派的那個
  旗標（非零走 AI），清成 0 之後那一隻改由玩家下指令。
  `+84h` 的位元 7 與 `+10Ch == 1` 是兩個例外，不會被交回。

## entry 8（`10DDh`）是士氣

```
10e7  entity[+14h] = 0
10f4  0100h:0066h(記錄)
10ff  entity[+10h] != 0 → entity[+14h] = 1，印 "is forced to flee"，這一隻的
                          回合就結束
113e  記錄 +84h <= 7Fh → 直接返回
114c  DS:6783h = (記錄 +84h & 7Fh) × 2，再與 66h 比
```

字串在 `10C0h`（`is forced to flee`）與 `10D2h`（`Surrenders`）。
所以 `entity[+14h]` 是「這一隻正在逃」、`entity[+10h]` 是士氣判定的結果。

## entry 3（`03E3h`）是「用身上的東西」

```
03f9  次數 = Roll(1, 7)
0407  entity[+2] == 0                → 返回
0419  side = overlay-25 entry 30(記錄) ; 對立陣營值
0427  DS:[6772h + side] <= 0          → 返回   ; 那一邊沒人站著
0431  [4933h]+1CAh != 0               → 返回
0440  重複「次數」次：
0458    物品 = 記錄 +C8h              ; 物品串列的頭
047e    [bp-8] = 物品 +3Dh
0488    型別 = 物品 +2Eh，查 DS:54E0h + 型別 × 16 → [bp-7]
049e    00E2h:003Eh(物品) 非零        → 跳過
04ad    物品 +3Eh >= 80h              → 跳過
04b7    物品 +34h == 0                → 跳過
04c1    [bp-8] <= 0                   → 跳過
04c7    [bp-8] > 38h                  → [bp-8] −= 17h
```

走的是記錄 `+C8h` 的物品串列、查的是物品型別表（spec 065：那張表的真身在
ZIP 的 `poolrad/items`，`+2Eh` 就是索引），所以這一支是**怪物用身上的
魔法物品**。`+3Dh` 那個值大於 `38h` 時減 `17h`，看起來是把物品上的編號
換算成法術編號。

## 還沒讀

- entry 2（`0203h` 施法）的內容，以及戰術模式 1..6 分別對到哪一支。
- entry 3 裡的 `00E2h:003Eh`、物品 `+34h`／`+3Dh`／`+3Eh` 的欄位語意，
  以及 `[4933h]+1CAh` 是什麼。
- `0100h:0066h`（overlay-24）在士氣判定裡算什麼；`+84h` 的位元 7 與
  `DS:6783h` 那個 × 2 之後與 `66h` 的比較。
- 型別表 `31A3h + 型別 × 16` 的 `+1`／`+2` 欄位語意（`+0` 是體型類別）。
