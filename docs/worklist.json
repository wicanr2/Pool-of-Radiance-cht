{
  "schema": "pool-worklist/1",
  "note": "未完成項的權威在這一份。WORKLIST.md 兩個 `worklist:begin`／`worklist:end` 標記之間那一段由 `cmd/pool-worklist -mode render -write WORKLIST.md` 產生，不要手改；標記外的段落是手寫的，工具不會動。每一項的 verify 跑起來為真代表『這一條仍然未完成』——為假就是東西做好了而清單沒改，也就是過期斷言。`json_len` 與 `json_gap` 是同一份資料的兩個方向：前者「≤ max 才算未完成」給進度型用（涵蓋幾張圖，越多越好），後者「≥ min 就算未完成」給缺口型用（幾份規格沒人指回去，越少越好）。",
  "layers": {
    "feature": "玩家會撞到的功能缺口",
    "verification": "驗證缺口：接了，但沒拿原版當裁判驗過",
    "presentation": "版面與資料的差距"
  },
  "items": [
    {
      "id": "wandering-monsters",
      "layer": "feature",
      "title": "走路時沒有人擲遭遇",
      "body": "`encounter.go` 自承 remake 沒有遊蕩遭遇，目前的戰鬥全部掛在格子事件底下。**這一條有數字**：起始地圖 GEO3／0 的 1024 個「座標×朝向」樣本各跑一次格子生命週期，沒有任何一格走到 `24h COMBAT`（`docs/audit/pool-initial-cell-sweep.json`）。\\n\\n**2026-09-11 查清楚了一半**：貧民窟的遭遇**排得出來**——由那一區的入口 3（紮營被打斷）跑，隊伍強度決定數量與分幾群，怪物編號從腳本自己的五張三格表查（spec 136，`slum_encounter_test.go` 實跑排出三群）。所以缺的不是「遭遇表沒接」，是**走路時沒有人去擲那一次**：入口 0 從頭到尾不碰 `24h COMBAT`。",
      "acceptance": "先確認原版走路到底會不會遇到怪（dosgolem 實跑，走固定步數看有沒有遭遇）。會的話把觸發點讀出來寫進 spec 136 再接上，並在戰術盤面以 `encounterStartDistance` 的距離開場；不會的話把這一條關掉並在 spec 136 寫明。",
      "verify": {
        "kind": "present",
        "paths": [
          "cmd/pool-game/encounter.go"
        ],
        "pattern": "remake 還沒有地圖上的怪物群",
        "note": "程式裡的自承還在"
      },
      "blocked_by": "走路的遭遇由誰擲還沒讀。兩種可能要先分開：引擎（overlay）擲完再呼叫某個入口，或者貧民窟本來就只有紮營會被打擾。**沒分開之前不能斷定這是 remake 的缺口**——照攻略貧民窟走路會遇到怪，但那要用 dosgolem 實跑確認，不是拿印象當證據。"
    },
    {
      "id": "thief-skill-source",
      "layer": "feature",
      "title": "賊技能沒有產生端",
      "body": "記錄 `+77h` 起八格（spec 095）在建角與升級都沒有產生端。**非賊角色的 0 是正確答案不是佔位**（spec 095 自己註明），缺的是賊角色那一份。",
      "acceptance": "把產生端寫進 spec 095 並移出 DRAFT，再讓建角與升級算得出來，存檔欄位不再是常數 0。",
      "verify": {
        "kind": "present",
        "paths": [
          "cmd/pool-game/checkparty.go"
        ],
        "pattern": "remake 還沒有",
        "note": "程式裡的自承還在"
      },
      "blocked_by": "spec 095 的 DRAFT：**原版沒有任何 overlay 寫這八格**，所以值從哪來還沒解出來。\n下一步不是反組譯而是實測——用 dosgolem 跑原版建一個賊，把存檔的 `+77h` 起八格讀出來比對規則書的表；值若跟著等級與種族動，產生端就在建角那條路上。"
    },
    {
      "id": "level-drain-source",
      "layer": "feature",
      "title": "能量吸取沒有來源（缺的是特殊攻擊那一批）",
      "body": "欠帳那對欄位（記錄 `+74h`／`+75h`）與還帳（恢復術走 `gamepack.Restore`）**都接好了**，spec 097 是 CONFORMED、`21C4h..2286h` 逐行讀完了。缺的是**誰來扣**：原版由怪物的特殊攻擊呼叫 `21C4h`，而 remake 還沒有特殊攻擊那一批。所以 `DrainedLevels` 一律是 0——**空的是正確答案不是佔位**（沒欠帳時原版的 `2C16h` 也是直接返回），程式裡的註解已經這樣寫。",
      "acceptance": "把特殊攻擊的分派讀出來寫進 spec 並標 READY，接上之後吸取扣得動等級，復原走神殿那條路。",
      "verify": {
        "kind": "present",
        "paths": [
          "internal/save/state.go"
        ],
        "pattern": "remake 還沒有吸取的來源",
        "note": "程式裡的自承還在"
      },
      "blocked_by": "特殊攻擊的分派還沒讀。線索在 spec 065：「第一形態空的那六隻怪物，牠們的第一形態是特殊攻擊」——所以怪物記錄的兩個攻擊槽裡，空骰子的那一格代表走特殊攻擊，但走去哪一支、怎麼挑，都還沒定位。"
    },
    {
      "id": "combat-numbers-vs-original",
      "layer": "verification",
      "title": "戰鬥數值對原版",
      "body": "敵方 AI 已換成原版的接近規則，命中、傷害、豁免與存活**沒有在同一場戰鬥裡對過原版**。",
      "acceptance": "同一組人物、同一場遭遇、固定骰子種子，逐回合比 THAC0、傷害骰結果與雙方 HP 變化；不一致的逐條記進 spec，不留「大致相符」。",
      "verify": {
        "kind": "manual",
        "note": "沒有機器可判的訊號。接上逐回合比對的測試之後，把這一條改成綁那條測試存不存在"
      }
    },
    {
      "id": "main-line-end-to-end",
      "layer": "verification",
      "title": "主線從開場到結局連續跑過一次",
      "body": "破關那一場打得贏、旗標也立得起來（2026-09-03，`ECL5/7`），但那是單點驗證，不是一趟。",
      "acceptance": "一次不中斷的實跑，每一個必經 block 都留下經過紀錄；中途卡住的地方寫成可重跑的測試。",
      "verify": {
        "kind": "manual",
        "note": "沒有機器可判的訊號。寫成可重跑的實跑測試之後，把這一條改成綁那條測試"
      }
    },
    {
      "id": "real-machine-startup",
      "layer": "verification",
      "title": "Windows 與 macOS 的真機啟動結果回填",
      "body": "逐步清單已經寫好交接出去（[`docs/verification/real-machine-startup-checklist.md`](docs/verification/real-machine-startup-checklist.md)），**結果還沒寫回來**。Wine 與 Docker 證得了「不是連跑都跑不起來」，證不了真機。",
      "acceptance": "把七步的結果與每台三張截圖寫回那份清單。",
      "verify": {
        "kind": "manual",
        "note": "要別人在真機上跑完回填。checklist 目前沒有結構化的結果欄位，所以沒得驗"
      }
    },
    {
      "id": "guide-other-maps",
      "layer": "presentation",
      "title": "遊戲內攻略擴到其餘地圖",
      "body": "`F3` 的機制與資料格式做好了（[`docs/guide/README.md`](docs/guide/README.md)），目前只有 `3/00` 費蘭的文明區建了 48 個點。",
      "acceptance": "每一張新地圖的點都由原始 GEO／ECL 推出來、每一個點帶 `source`，且 `internal/guide` 的三個測試全綠——**不可以抄第三方攻略的座標**，那種錯的症狀是「測試綠、玩家走不到」。",
      "verify": {
        "kind": "json_len",
        "path": "internal/guide/zh-TW.json",
        "field": "maps",
        "max": 1,
        "note": "攻略只涵蓋 1 張圖（3/00）時這一條仍未完成；多一張這裡就會開口"
      }
    },
    {
      "id": "parity-sampling-gaps",
      "layer": "presentation",
      "title": "抽樣擴到還沒對拍的畫面",
      "body": "現況表有二十項；原版也有、remake 也做了、但還沒進抽樣的清單在 [`docs/audit/dos-parity-sample.md`](docs/audit/dos-parity-sample.md) 的〈還沒進抽樣的〉——紮營底下那九層、商店、神殿、挑法術頁、裝備、地圖上施法、平面圖。",
      "acceptance": "每加一項都照市政廳那一節的做法（另產一組基準或延長主鍵序、compare 的 plan 加一項），`docs/audit/dos-parity-sample.json` 跟著多一項。",
      "verify": {
        "kind": "json_len",
        "path": "docs/audit/dos-parity-sample.json",
        "field": "screens",
        "max": 20,
        "note": "綁數量而不是綁註解：註解會被順手改掉，項數不會。**`max` 要跟著現況走**——每加一項就把它加一，這樣條目在清單還沒清空之前一直有效。"
      }
    },
    {
      "id": "ecl-operand-count-misread",
      "layer": "verification",
      "title": "ECL 還有一處運算元數讀錯",
      "body": "`TestPlayingTheWorldCompletesCommissionsOnItsOwn` 的隨機探索在 2026-09-10 撞到一次 `continue Pool SearchLocation: unknown opcode 0x9D at payload offset 1085`（seed 142，第 21526 步）。**`9Dh` 不是 opcode**——那是 PC 停在資料上，症狀與 spec 093 修掉的 `34h ECL CLOCK` 同一族：某一條指令的運算元數讀多了，後面整段錯位。同一份測試重跑一次沒有再現，所以是特定路徑才走得到。",
      "blocked_by": "要先定位是哪一個 block 的哪一條指令。`payload offset 1085` 是線索，但那一趟走過 22 張地圖，哪一張的哪一個 block 還沒對出來；重現要靠同一組 seed 與同一條路徑。",
      "acceptance": "定位那一條指令、對回原版的運算元數、寫進 spec，並讓那個 seed 重跑不再硬失敗。",
      "verify": {
        "kind": "manual",
        "note": "沒有穩定訊號——隨機探索才撞得到，重跑會消失。**不要因為重跑是綠的就當它不存在**：門檻「硬失敗 0 次」在隨機探索下量到的是這一次走了哪條路，不是缺口在不在。定位到之後改成綁那個 block 的靜態走訪"
      }
    },
    {
      "id": "castle-test-runtime",
      "layer": "verification",
      "title": "城堡那條探索測試要七分半",
      "body": "`TestTheCastleBehindStojanowGateHasContent` 單獨跑量到 458 秒，而整個 `cmd/pool-game` 在 380..600 秒之間浮動。go 的預設 timeout 是 10 分鐘，所以這個套件長期在邊緣——2026-09-10 撞上一次，症狀是 `panic: test timed out`，**看起來像當掉而不是慢**。`tools/go.sh` 已經改成預設 `-timeout 25m` 讓它不再被誤砍，但那只是不再誤判，沒有變快。",
      "blocked_by": "",
      "acceptance": "把它壓到兩分鐘以內，做法照 Sokal 那條的前例（縮小探索面、去掉重複走訪），而不是放寬它檢查的東西。",
      "verify": {
        "kind": "manual",
        "note": "沒有機器可判的訊號——耗時要實測。壓下去之後改成綁一條會在超時時失敗的斷言"
      }
    },
    {
      "id": "parity-not-reproducible",
      "layer": "verification",
      "title": "對拍跑兩次結果不一樣",
      "body": "2026-09-10 連跑三次同一個 AppImage 對同一組基準：**兩次走位失敗**（卡在市政廳那一段的 `await adventure-cell-menu`，停在 `adventure-move`），成功那兩次的截圖**逐像素比對有六張不同**。差異分兩組，各自跨三張同類畫面：狀態列右側那一格（邏輯座標 x=526..597、y=119..127，時鐘）差 284 個像素；人物資料頁主體（x=69..565、y=51..295，擲出來的數值與肖像）差 1896 個像素。所以 `dos-parity-sample.json` 的數字每次跑都會浮動十個像素上下——**小幅變動不代表回歸，也不代表沒有回歸**，那一欄現在分不出來。\n\n**第三個來源（2026-09-11）**：紮營的營火是兩張動畫，兩邊的截圖各自停在哪一張由時機決定，視野那一欄因此在 100% 與 91.27% 之間跳（676 格）。這一個查清楚了，不是缺陷；剩下要固定的仍是時鐘與建角擲值。",
      "blocked_by": "要固定的是兩件事：建角的擲值與肖像（現在跟著時間走）、以及畫面上的時鐘。前者可能要讓 AppImage 吃一個 seed 參數，後者要嘛不入鏡要嘛固定起始時刻——兩個都會動到發行包的介面，不是只改腳本。 營火那一項另外算：它要嘛在截圖前停住動畫，要嘛接受兩個值——後者會讓這一欄失去「掉了就是回歸」的意義，所以先不接受。",
      "acceptance": "同一個 AppImage 對同一組基準連跑三次：三次都要走完，而且二十張截圖逐位元組相同。做到之後 `dos-parity-sample.json` 的數字才有「掉了就是回歸」的意義。",
      "verify": {
        "kind": "manual",
        "note": "沒有現成訊號可綁——要跑三次對拍才知道穩不穩。接上之後改成綁一支跑三次比雜湊的腳本，或在 `parity.json` 裡記下重跑的雜湊一致與否"
      }
    },
    {
      "id": "effect-expiry-teardown",
      "layer": "feature",
      "title": "效果代碼的處理常式只接了一個",
      "body": "到期那一刻的入口**已經接上了**：原版 `01B8h` 呼叫的 `0100h:002Ah` 就是 **overlay-24 entry 2**（`0100h` 是 overlay-24 的 stub 段，`(offset − 20h) ÷ 5` 換回進入點，spec 109／112）——`+4` 非 0 的節點先用**模式 1** 叫一次自己代碼的處理常式，再摘掉。remake 這一側是 `(*app).expiredEffectTeardown`，戰場那一側是 `tacticalState.effectTeardown`，兩條路同一個入口。\n\n**缺的是代碼本身。** 原版填了 138 格（代碼 1..139，136 個落在 overlay-12、2 個在 overlay-13，spec 112），remake 有具名常數的只有十個，真正實作過收尾的只有 `28h`（雲團）。所以現在地圖上到期就只是摘掉節點——**這對已實作的代碼是對的**，對還沒實作的代碼則是「還沒接」與「不必接」分不出來。",
      "blocked_by": "138 個代碼不是一次做完的工作，要按玩家碰得到的順序挑。挑之前缺一份盤點：哪些代碼玩家在這一版走得到、哪些已經由別的機制涵蓋（例如神殿那九項服務直接改`Effects`，不走處理常式）。",
      "acceptance": "先產出代碼覆蓋盤點（原版 138 格 vs remake 實作），再按玩家可見度接。每接一個代碼要有走玩家路徑的測試：掛上、時間過去、狀態真的回復。",
      "verify": {
        "kind": "present",
        "paths": [
          "cmd/pool-game/combat_effects.go"
        ],
        "pattern": "地圖上目前一個代碼都不需要動作",
        "note": "綁在 `expiredEffectTeardown` 的自承註解上：第一個在地圖上有動作的代碼接上來時，那句話就得拿掉"
      }
    },
    {
      "id": "spec-test-backlinks",
      "layer": "verification",
      "title": "五十八份規格沒有任何測試指回它",
      "body": "`docs/audit/doc-index.json` 的 `specs_without_tests` 有 58 份。實作那一欄剛剛清完（每一份要嘛有檔案的註解指回它，要嘛在檔頭寫明實作不是 Go），測試這一欄還沒。\\n\\n**這不等於沒測**：測試可能存在，只是註解裡沒寫 `spec NNN`，那條線就接不起來。所以逐份要分三種處置——補註解（測試已經在測它）、補測試（真的沒測）、或在檔頭寫明為什麼不需要（純 RE 文件、實作在別的 repo）。",
      "blocked_by": "分不出「沒測」與「測了但沒標」之前不能動手：前者要寫測試，後者只要一行註解，而兩者在這個數字上長得一樣。",
      "acceptance": "`specs_without_tests` 歸零，而且每一份的處置要看得出是哪一種。不准為了讓數字下降而在無關的測試裡加 `spec NNN`。",
      "verify": {
        "kind": "json_gap",
        "path": "docs/audit/doc-index.json",
        "field": "specs_without_tests",
        "min": 1,
        "note": "綁 `cmd/pool-doc-index` 算出來的清單長度。`min` 是 1 不是現況值——這一條要的是歸零"
      }
    }
  ]
}
